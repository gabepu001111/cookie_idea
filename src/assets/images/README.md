Place your own images here to replace the placeholders used by `LandingPage.jsx`.

- Filenames currently used by the app (replace these exact names):
  - image1.jpeg
  - image2.svg
  - image3.svg
  - image4.svg
  - image5.svg
  - image6.svg
  - image7.svg
  - image8.svg

Tips:
- For best results use images sized around 800×600 (or larger) and keep them in JPG/PNG/WebP/SVG formats.
- If you add JPEG/PNG files, you can keep the same filenames (e.g. `image1.jpg`) but you must update the imports in `src/components/LandingPage.jsx` accordingly or keep the .svg placeholders and overwrite them with your own .svg files.
- After replacing the files, restart the dev server (npm run dev) if it was running to pick up changes.
